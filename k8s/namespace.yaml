apiVersion: v1
kind: Namespace
metadata:
  name: vdb-system
  labels:
    app.kubernetes.io/name: cuda-vector-db
    app.kubernetes.io/version: "v1.0.0"
  annotations:
    description: "CUDA Vector Database System"